---
title: Get Started
description: How to run the BlackRoad OS portal locally.
---

## Running locally

```bash
pnpm install
pnpm dev
```

Visit `http://localhost:3000` to see the portal.

## Static export

```bash
pnpm build
```

The static site is emitted to `.out`. Serve it with `pnpm start`.
