# blackroad-os-web
Create a minimal API service:  - app/: FastAPI with a simple /health and /version endpoint - schemas/: placeholder pydantic models - infra/: Dockerfile, requirements.txt, sample railway.toml (no secrets) Keep it extremely small and clean, just enough structure to grow.
